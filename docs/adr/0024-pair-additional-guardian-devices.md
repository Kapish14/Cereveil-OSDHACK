# Pair additional guardian devices

An existing authenticated Guardian Device adds another device by generating a short-lived, single-use Guardian Pairing Code normally displayed as a QR code. The new device receives its own independently revocable session on the same Guardian Account, avoiding shared passwords while preserving equal authority across at most two active Guardian Devices.
