# Force safe search or block the engine

Safe Browsing will use provider-supported DNS mappings to force strict filtering for search engines such as Google and Bing. Because DNS filtering cannot inspect encrypted search queries, known search engines without an enforceable safety mode will be blocked while Safe Browsing is enabled; NSFW Screen Detection remains a secondary reactive safeguard rather than the primary search filter.
