# Enroll child devices with short-lived codes

Child Profiles will not have reusable passwords or a Child Mode logout action. A Guardian binds a Child Device using a short-lived, single-use Enrollment Code; subsequent removal requires Guardian Approval or a Recovery Code, reducing credential leakage and preventing the Child from casually escaping enrollment.
