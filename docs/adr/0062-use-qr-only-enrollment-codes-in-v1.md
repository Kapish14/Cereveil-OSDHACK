# Use QR-only Enrollment Codes in v1

Enrollment Codes will be presented and scanned through QR codes only in v1, without a human-typeable fallback. This keeps the first Child Device enrollment flow focused on same-room pairing and avoids adding manual entry UI, formatting rules, retry limits, and brute-force protections before there is evidence that QR scanning is insufficient.
