# Warn the child and alert the guardian together

Every positive AI safety detection will simultaneously create a Child-facing Safety Warning and a Guardian-facing Safety Alert. Scam Text Detection uses a warning overlay, while NSFW Screen Detection blurs detected content in real time and shows a warning; detailed capture cadence, region tracking, model thresholds, and overlay rendering remain deferred until implementation design.
