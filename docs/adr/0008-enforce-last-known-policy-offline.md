# Enforce the last known policy offline

When a Child Device is offline, Child Mode continues enforcing its last accepted Supervision Policy and running available on-device safety detection. New analytics and safety events queue locally for later synchronization, while Guardian Mode shows previously synchronized location and analytics with explicit timestamps; live location, Remote Audio Sessions, and new remote commands remain unavailable until connectivity returns.
