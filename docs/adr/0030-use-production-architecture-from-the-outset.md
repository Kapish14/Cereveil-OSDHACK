# Use production architecture from the outset

Cereveil will use its intended production architecture from the first implementation, while delivering only the hackathon feature subset initially and adding deferred features later. The hackathon may reduce product scope but will not introduce a separate prototype backend, data model, authentication scheme, or integration design that must later be replaced; this costs more engineering effort up front but avoids validating a safety product on disposable foundations.
