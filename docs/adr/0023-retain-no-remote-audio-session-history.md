# Retain no remote-audio session history

After a Remote Audio Session ends, Cereveil will retain neither audio nor session metadata and will provide no session-history view, mirroring the decision not to retain location history. This minimizes sensitive records but deliberately means neither the Guardian nor Child can later verify which Guardian Device initiated a session or when it occurred.
